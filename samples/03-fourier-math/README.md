# 03 — Fourier series math

General Fourier series formula, then morphed into the explicit first terms.

**⚠ Requires LaTeX (xelatex on PATH).** Skip this sample if you installed manim with `--skip-latex`.

**Techniques:** `MathTex`, `Write`, `ReplacementTransform`, `Indicate`.

**Render:**
```bash
python -m manim render -qm samples/03-fourier-math/scene.py FourierMathScene
```

**Files:** `storyboard.yaml` (2 scenes, 12s), `scene.py` (`FourierMathScene`), `out.mp4`, `thumb.png`.
