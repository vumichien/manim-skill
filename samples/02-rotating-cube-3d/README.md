# 02 — Rotating cube (3D)

Translucent cube spins around the Z axis with reference axes shown.

**Techniques:** `ThreeDScene`, `set_camera_orientation`, `ThreeDAxes`, `Cube`, `Rotate(... axis=UP)`. **No LaTeX required.**

**Render:**
```bash
python -m manim render -qm samples/02-rotating-cube-3d/scene.py RotatingCube3DScene
```

**Files:** `storyboard.yaml` (1 scene, 8s), `scene.py` (`RotatingCube3DScene`), `out.mp4`, `thumb.png`.
