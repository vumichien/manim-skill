# 01 — Pythagoras 2D

Visual proof of `a² + b² = c²` using squares-on-sides construction.

**Techniques:** `Polygon`, `Square`, `Text`, `Create`, `Write`, `LaggedStart`, `Indicate`. **No LaTeX required.**

**Render:**
```bash
python -m manim render -qm samples/01-pythagoras-2d/scene.py Pythagoras2DScene
```

**Files:** `storyboard.yaml` (3 scenes), `scene.py` (`Pythagoras2DScene`), `out.mp4`, `thumb.png`.
