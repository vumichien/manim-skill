# 05 — Text morph

The word **idea** morphs into **animation** then fades.

**Techniques:** `Text`, `Write`, `ReplacementTransform`, `FadeOut`. **No LaTeX required.**

**Render:**
```bash
python -m manim render -qm samples/05-text-morph/scene.py TextMorphScene
```

**Files:** `storyboard.yaml` (1 scene, 6s), `scene.py` (`TextMorphScene`), `out.mp4`, `thumb.png`.
