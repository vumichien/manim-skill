# 04 — Quadratic plot

Plot `y = x²` on labeled axes.

**Techniques:** `Axes`, `axes.plot(lambda x: x**2)`, `add_coordinates`, `Create`, `Write`. **No LaTeX required.**

**Render:**
```bash
python -m manim render -qm samples/04-quadratic-plot/scene.py QuadraticPlotScene
```

**Files:** `storyboard.yaml` (1 scene, 8s), `scene.py` (`QuadraticPlotScene`), `out.mp4`, `thumb.png`.
